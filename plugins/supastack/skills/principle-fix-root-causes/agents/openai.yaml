interface:
  display_name: "Principle Fix Root Causes"
  short_description: "Run this Supastack workflow in Codex"
  default_prompt: "Use $supastack:principle-fix-root-causes for this request."
policy:
  allow_implicit_invocation: false
