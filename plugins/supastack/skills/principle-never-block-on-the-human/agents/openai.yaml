interface:
  display_name: "Principle Never Block On The Human"
  short_description: "Run this Supastack workflow in Codex"
  default_prompt: "Use $supastack:principle-never-block-on-the-human for this request."
policy:
  allow_implicit_invocation: false
