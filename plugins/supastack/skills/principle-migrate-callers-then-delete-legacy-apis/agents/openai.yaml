interface:
  display_name: "Principle Migrate Callers Then Delete Legacy Apis"
  short_description: "Run this Supastack workflow in Codex"
  default_prompt: "Use $supastack:principle-migrate-callers-then-delete-legacy-apis for this request."
policy:
  allow_implicit_invocation: false
