interface:
  display_name: "Principle Minimize Reader Load"
  short_description: "Run this Supastack workflow in Codex"
  default_prompt: "Use $supastack:principle-minimize-reader-load for this request."
policy:
  allow_implicit_invocation: false
