# Repository instructions

- After completing and verifying a task that changes this repository, create an atomic commit without waiting for a separate commit request.
- Stage and commit only the files or hunks changed for the current task. Never include pre-existing or unrelated user changes.
- Do not commit when the user explicitly asks to leave the changes uncommitted.
